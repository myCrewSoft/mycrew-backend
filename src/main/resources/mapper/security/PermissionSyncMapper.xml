<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mycrewsoft.security.rbac.PermissionSyncMapper">

    <insert id="mergePermissions">
        MERGE INTO TB_PERMISSION P
        USING (
            SELECT
                (SELECT NVL(MAX(PERM_ID), 0) FROM TB_PERMISSION)
                    + ROW_NUMBER() OVER (ORDER BY PERM_CD) AS PERM_ID,
                PERM_CD,
                PERM_NM,
                PERM_EXPLN,
                'Y' AS PERM_ENABLED
            FROM (
                <foreach collection="permissions" item="permission" separator=" UNION ALL ">
                    SELECT
                        #{permission.code} AS PERM_CD,
                        #{permission.name} AS PERM_NM,
                        #{permission.description} AS PERM_EXPLN
                    FROM DUAL
                </foreach>
            )
        ) SRC
           ON (P.PERM_CD = SRC.PERM_CD)
         WHEN NOT MATCHED THEN
           INSERT (
               PERM_ID,
               PERM_CD,
               PERM_NM,
               PERM_EXPLN,
               PERM_ENABLED
           )
           VALUES (
               SRC.PERM_ID,
               SRC.PERM_CD,
               SRC.PERM_NM,
               SRC.PERM_EXPLN,
               SRC.PERM_ENABLED
           )
    </insert>
</mapper>
