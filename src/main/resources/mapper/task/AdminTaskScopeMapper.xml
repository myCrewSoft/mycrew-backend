<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycrewsoft.domain.task.mapper.AdminTaskScopeMapper">

    <resultMap id="AdminScopeOptionResultMap" type="com.mycrewsoft.domain.employee.dto.response.AdminScopeOptionResponseDTO">
        <result property="scopeId" column="SCOPE_ID"/>
        <result property="scopeCode" column="SCOPE_CODE"/>
        <result property="scopeName" column="SCOPE_NAME"/>
        <result property="label" column="LABEL"/>
    </resultMap>

    <select id="selectTaskScopeOptions" resultMap="AdminScopeOptionResultMap">
        SELECT
            TO_CHAR(T.TASK_ID) AS SCOPE_ID,
            TO_CHAR(T.TASK_ID) AS SCOPE_CODE,
            T.TASK_NM AS SCOPE_NAME,
            TO_CHAR(T.TASK_ID) || ' - ' || T.TASK_NM AS LABEL
        FROM TB_TASK T
        WHERE NVL(T.DEL_YN, 'N') = 'N'
        ORDER BY T.TASK_NM, T.TASK_ID
    </select>
</mapper>
